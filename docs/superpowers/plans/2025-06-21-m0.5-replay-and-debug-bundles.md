# M0.5: Replay CLI & Debug Bundles — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans or subagent-driven-development.

**Goal:** Ship `tools/replay` CLI and complete debug bundle export so session issues can be diagnosed without X4.

**Depends on:** M0 complete. Specifically requires:
- `data/sessions/<id>/event.log` NDJSON format + writer (M0 Task 3, Step 4b — `server/src/event_log.rs`).
- `server/src/debug_bundle.rs` stub (M0 Task 8) and the seq-stamped `EventEnvelope` line format.

If M0's event log task was skipped, stop and implement it first — replay and bundles have no data source without it.

**Design spec:** `docs/superpowers/specs/2025-06-21-x4-multiplayer-design.md` § Observability

---

### Task 1: Debug bundle (full)

**Files:**
- Modify: `server/src/debug_bundle.rs`
- Test: `server/tests/debug_bundle.rs`

- [ ] On `event.rejected` or hash mismatch hook, write full bundle: `meta.json`, `last_events.jsonl` (last 200 lines tailed from the session's `data/sessions/<id>/event.log`), `acl_decisions.json`, `hashes.json`
- [ ] Test asserts directory created with expected files

### Task 2: Replay CLI crate

**Files:**
- Create: `tools/replay/Cargo.toml` (workspace member)
- Create: `tools/replay/src/main.rs`

- [ ] Add `replay` to workspace members in root `Cargo.toml`
- [ ] CLI: `x4mp-replay --session <uuid> --from-seq N --server ws://127.0.0.1:7878/ws`
- [ ] Read `data/sessions/<id>/event.log`, send each line as WS text after handshake
- [ ] Integration test with sample log against running server

### Done criteria

- `cargo test -p x4mp-server --test debug_bundle` PASS
- `cargo run -p x4mp-replay -- --help` works
- Manual replay of M0 sample log reproduces seq progression
